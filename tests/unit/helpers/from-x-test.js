import { fromX } from '../../../helpers/from-x';
import { module, test } from 'qunit';

module('Unit | Helper | from x');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = fromX(42);
  assert.ok(result);
});
