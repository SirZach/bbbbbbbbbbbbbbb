{{#each data as |bar|}}
  <div style="width: {{bar.width}};">
    {{#if bar.count}}
      <span class="{{bar.label}}">{{bar.count}}</span>
    {{else}}
      <span class="no-count">{{bar.count}}</span>
    {{/if}}
  </div>
{{/each}}
